# NBA大小分预测 - 快速开始 🚀

## 5分钟上手

### 1. 安装依赖（首次运行）

```bash
cd ~/projects/nba
pip install -r requirements.txt
```

### 2. 获取NBA数据

```bash
# 获取本赛季最近100场比赛数据
python scripts/fetch_data.py --season 2024-25 --games 100
```

输出示例：
```
📥 获取 2024-25 赛季比赛数据...
✅ 获取到 100 场比赛数据
💾 已保存: ~/projects/nba/data/raw/games_2024-25.csv
```

### 3. 分析数据

```bash
python scripts/analyze.py
```

### 4. 查看数据

```bash
# 查看比赛数据前5行
head -n 5 data/raw/games_2024-25.csv

# 统计文件行数
wc -l data/raw/games_2024-25.csv
```

## 常见问题

### Q: 安装nba_api报错？

```bash
# 如果pip安装失败，尝试：
pip install --upgrade pip
pip install nba_api --no-cache-dir
```

### Q: API请求超时？

nba_api有时会被限流，可以：
1. 减少`--games`参数
2. 在代码里加`time.sleep(1)`延迟

### Q: 数据存在哪里？

```
data/
├── raw/          # 原始CSV数据
│   ├── games_2024-25.csv      # 比赛数据
│   └── team_stats_2024-25.csv # 球队统计
├── processed/    # 清洗后数据（待实现）
└── features/     # 特征数据（待实现）
```

## 下一步

完成数据获取后，查看：
- `docs/ARCHITECTURE.md` - 系统架构
- `README.md` - 完整文档

---

**提示**: 第一次运行可能需要几分钟下载数据，请耐心等待。
